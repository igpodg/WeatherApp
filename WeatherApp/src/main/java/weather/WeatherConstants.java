package weather;

public class WeatherConstants {
    public enum DayOfWeek {
        TOMORROW,
        AFTER_TOMORROW,
        AFTER_AFTER_TOMORROW
    }

    public enum TemperatureFormat {
        CELSIUS,
        FAHRENHEIT
    }
}
